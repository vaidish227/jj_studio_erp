// Reserved for V3 — a human-readable catalog that can be injected into the
// system prompt for very small models that don't follow OpenAI tools spec.
// In V1 we pass tool definitions via the OpenAI `tools` parameter directly,
// so this stays empty intentionally.

module.exports = {};
